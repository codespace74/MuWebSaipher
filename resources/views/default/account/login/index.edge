@include('default/header/index')
<div class="content" id="content">
	<h1 class="title"><span>Login</span></h1>
	<form class="loginForm" method="POST" action="{{ route('authAccount') }}">
		{{ csrfField() }}
		{{ elIf('<span>$self</span>', getErrorFor('username'), hasErrorFor('username')) }}
		{{ elIf('<span>$self</span>', getErrorFor('password'), hasErrorFor('password')) }}

		<div class="formGroup">
			<span>Username:</span>
			<input type="text" name="username" class="login" placeholder="Username">
		</div><!-- formGroup -->
		<div class="formGroup">
			<span>Password:</span>
			<input type="password" name="password" class="pass" placeholder="Password">
		</div><!-- formGroup -->
		<div class="formButton">
			<button>LOGIN</button>
		</div>
	</form>
</div><!-- content -->
</form>
@include('default/footer/index')